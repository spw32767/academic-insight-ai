# article-classification

This task classifies academic articles from `title` and `abstract`.

Owned by this task folder:
- categories (`config.py`)
- prompt and correction prompt (`prompt.py`)
- output schema (`schema.py`)
- task execution logic (`runner.py`)

Do not move task-specific prompt/categories/schema into global folders.
