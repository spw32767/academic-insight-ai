# Database module (future)

This module is reserved for future database integration.

Planned flow:
1. Read source articles from the existing database.
2. Run selected AI task and model.
3. Persist raw and validated results into AI result logs.
4. Optionally write back selected final category.

Current version does not connect to a real database yet.
